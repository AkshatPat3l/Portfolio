import React from 'react';

const Hero = () => {
    return (
        <header className="hero">
            <div className="hero__container">
                <h1 className="hero__title">Welcome to Our Website</h1>
                <p className="hero__subtitle">This is a simple website template.</p>
            </div>
        </header>
    );
};

export default Hero;